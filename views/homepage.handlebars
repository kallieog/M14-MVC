<main>
    {{#each posts as |post|}}
    <div class="card">
        <div class="card-header">
            <a href="/posts/{{id}}">
                <h3 class="card-title">
                    {{title}}
                </h3>
            </a>
        </div>
    </div>

    {{/each}}
</main>